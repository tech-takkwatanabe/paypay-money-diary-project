---
description: フロントエンド・バックエンドの開発サーバーを起動します。
---

下記のステップに従って、開発サーバーを起動してください。

1. **開発サーバーの起動**: プロジェクトルートで `pnpm dev` を実行します。
   - これにより Turborepo 経由で `apps/web`（Next.js）と `apps/api`（Hono）の両方が起動します。
2. **起動確認**: サーバーが正常に起動したことをログから確認し、アクセス URL をユーザーに報告します。
