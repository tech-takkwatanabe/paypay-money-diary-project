---
description: pnpm lint と pnpm test を順番に実行し、コードの品質チェックを行います。
---

// turbo-all

下記のステップに従って、リント・テストを実行してください。

1. **リントの実行**: `pnpm lint` を実行し、結果を確認します。
2. **テストの実行**: `pnpm test` を実行し、結果を確認します。
3. **結果の報告**: エラーがあった場合は、対象ファイルとエラー内容をユーザーに報告します。
